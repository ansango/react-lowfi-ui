const base = "rounded-full";

export const size = {
  default: `px-3 py-3`,
};

export const outline = {
  primary: `${base} bg-white text-gray-800 border-2 border-gray-900`,
  secondary: `${base} bg-gray-100 text-gray-500 border-2 border-gray-400`,
};

export const solid = {
  primary: `${base} bg-gray-900 text-white`,
  secondary: `${base} bg-gray-400 text-gray-100`,
};
